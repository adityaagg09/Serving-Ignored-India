<%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/navigation.ejs') %>
    <% if(err) { %>
        <div>
            <%= err %>
        </div>
        <%} %>
    <form action="/login" method="POST" novalidate>
        <div>
            <label for="emaill">Email-id</label>
            <input type="email" name="email" id="emaill" value="<%= oldinput.email %>">
        </div>
        <div>
            <label for="pasl">Password</label>
            <input type="password" name="password" id="pasl" value="<%= oldinput.password %>">
        </div>
        <button type="submit">login</button>
        <input type="hidden" name="_csrf" value="<%= toki %>">
        <div>
            <a href="/reset">Reset Password</a>
        </div>
    </form>
<%- include('../includes/end.ejs') %>